# CLAUDE.md — Tsunagu 官網

給接手的 Claude / 開發者的專案脈絡。**動手改東西前先讀完這份。**

---

## 這是什麼

「台湾人と日本企業をツナグ」（Tsunagu）的官方網站。
Tsunagu 媒合想去日本留學／打工度假的台灣人，與想聘台灣人的日本企業。
對台灣的參加者**全程免費**。主辦是智囊和夥伴有限公司（Brain & Partner Taiwan Inc.）。

- Instagram：[@tw.tsunagu.jp](https://www.instagram.com/tw.tsunagu.jp/)（主要觸及管道，6,800+ 追蹤）
- 自 2023-06 起累計舉辦 87 場交流會，地點涵蓋台北、台中、高雄、東京、大阪

技術：**Astro 7（純靜態，無框架）+ GitHub Pages**。

---

## 使用者已拍板的決策

| 項目 | 決定 |
|---|---|
| 語言 | 繁中為主、日文次要（日文版尚未做） |
| 調性 | **明亮乾淨、日系、不要深色**（2026-09-13 定案：以 bpt ROOM logo 為主色）。可愛感交給角色，文字負責優雅 |
| 字體 | **沿用簡報用字**：BIZ UDPGothic（日）+ Noto Sans TC（繁中）+ Jost（拉丁，對應簡報的 Century Gothic） |
| 文字色 | **中性灰**（取自 logo 的 bpt／R／M 字色）。~~暖褐色~~ 2026-09-13 使用者改判「整體太粉」，全站改白底灰字 |
| 表單 | 一律走 Google Forms，**官網本身不蒐集任何個資** |
| 部署 | GitHub Pages + Actions |
| 四個目標 | 報名交流會／瀏覽合作企業／送出 Profile／看學長姐經驗 |

### 曾經試過但被否決的

- **jf open 粉圓**（圓體）：使用者覺得不符合品牌，已移除。不要再提議圓體。
- **深色 CTA 按鈕**（#C42D66 底白字）：已改成粉彩底＋褐字。

---

## 設計系統（v0.2）

全部 token 在 `src/styles/global.css` 的 `:root`。改色請改那裡，不要在頁面裡寫死。
**字級也 token 化了（2026-08-28）**：`--fs-3xs`(11)〜`--fs-3xl`(20)＋`--fs-stat`(38)，
新字級一律取既有階，不要再發明 12.5px 這種 0.5 級碎值。例外（clamp、手機標題階
24/22、iOS 搜尋框 16px）都在原地有註解。SVG 元件（角色、icon、頭像）的填色
吃 CSS 變數，改 :root 會同步。~~`--pink-400`~~ 已刪（主按鈕無底線）。
`--note-*` 現在指向粉主色系（無黃色）；`.notice` 仍是唯一可用它的元件。

### 配色（2026-09-13 定案：只有兩個主色＋灰）

使用者兩次指示：(1)「整體太粉，要像簡報那樣明亮乾淨，以 bpt ROOM logo 配色當主色」；
(2)「footer 字標的粉是對的，其他地方的粉都太深；整個頁面只要這兩個主色，hero 不要漸層」。

**兩個主色**：珊瑚粉 `--pink-300` **#F3A0AA**、天空藍 `--sky-300` **#7DD0F5**（logo 藍 #00B0F0 的 pastel）。
**沒有淡階**（2026-09-13 使用者：「其他深淺都不要調整」）。~~`--pink-50/100/200`、`--sky-50/100/200`~~
2026-09-19 已全部歸位並**刪除**（白→`--paper`、200→300），**不要再加回**；只有 `--pink-300`／`--sky-300`。
**主色對白只有 2.0／1.7:1，不能當文字**——顏色一律放在**底色與線條**，文字全灰。
唯一例外：**統計數字**（`.band .n`、`.numbers .n`）用粉主色——使用者明確指定，已知不過 3:1，
單位小字（人／場）維持灰以保可讀：

| 用途 | 做法 |
|---|---|
| 主按鈕 | 底 `--pink-300`＋**白字**（使用者 2026-09-13 指定；對比 2.0:1 為其決定）。無深色底線 |
| 次按鈕／標籤 | 白底＋**主色框**＋灰字（無淡底） |
| 連結 | 內文 `a`、`.co-link`、`.rel-link`：灰字＋**藍主色 2px 底線**，hover 換粉底線。**`.action-link`（箭頭連結）＝淺藍主色字無底線**（使用者 2026-09-13 指定，與內文粗體區分；1.7:1 為其決定），hover 換粉字 |
| 當前導覽 | 抽屜與語言選單的當前項＝**粉字＋粗體**（規則 11；~~`.navlink.on`／`.mobile-tabs a.on`~~ 已隨分頁列移除） |
| 統計數字 | **粉主色**（使用者指定）；單位小字灰。實績行、場次編號等其他數字＝灰 |
| 引號「」、※、advice 短線 | `--pink-300`（裝飾） |
| `.notice` 必讀 | 白底＋`--pink-300` 框（**不再用黃色**，全站無第三色） |
| 品類 icon、頭像、圓形號碼章 | **主色細線條＋白填色**（logo 的細線語言）。實色填滿＋深灰線稿疊起來會「重」，使用者已兩次退回。頭像人物以 clipPath 圓形裁切在環內（r=25）並放大 1.08 填滿 |
| 篩選 chip | 未選中：白底＋1px 灰框、38px 高（輕）；選中：主色實底＋灰粗字＋**保留灰框**（2026-09-25 使用者；主色框對白不到 3:1）。層級是「選中比未選中重」 |
| 一行一筆的清單（舉辦紀錄、媒體報導） | 分隔線清單，**不做逐筆卡片**（12 個灰框疊成牆） |
| 大型 CTA 盒（.cta-band／.free）、品牌區、企業卡色帶 | **白底**＋1px 細線。使用者 2026-09-13：灰底讓版面不乾淨，全站不用 greige 當區塊底（greige 只剩 hover）。**唯一例外：/companies 的卡片列表區**（2026-09-14 使用者：35 張白卡在白底上眼花）用 `--sky-wash`（藍主色 12% 淡階，全站唯一淡階，只准當**企業卡區**的底色：/companies 列表與首頁企業區 `.alt`）全寬底；卡片不加框線，白卡靠**淡藍陰影** `--card-float` 浮起（hover 加深；~~9/14 粉色光暈邊線 `--glow-pink`~~ 2026-09-26 使用者改選：粉暈碰淺藍底會混成髒粉灰），卡片內**地區標籤改灰框**，彩色只留圖標方塊框與職種標籤、圖標方塊框用品類主色 |
| 主按鈕文字 | **不加「→」**（2026-09-14 使用者：箭頭太多）；只有 `.action-link` 帶箭頭。外連 Google 表單的「↗」是另開視窗語意，保留 |
| meta 小字（實績行、場次編號、hero 副標） | `--ink-600` 次要灰，讓正文（900）與 meta（600）有層次 |
| focus ring | `--ink-900` |
| hero／頁首 | **純白，無漸層** |
| 區塊分隔 | **不用橫線**（2026-09-25 使用者）：首頁主視覺底線、企業區上下線、結尾區頂線、footer 頂線、內頁標題下線全數拿掉；靠留白＋企業區淺藍底色帶換段落 |

~~`--pink-700`／`--sky-700`~~ 2026-09-19 已**刪除**（曾是「名粉實灰」的過渡 token，9 處引用全改 `--ink-900`）。
**文字一律 `--ink-900`／`--ink-600`；線條／底色直接用 `--pink-300`／`--sky-300`。**


灰階：`--ink-900` #444444（對白 9.74:1；壓在粉主色 4.83、藍主色 5.66 ✓）、`--ink-600` #6E6E6E（5.10:1）、
`--ink-400` #A3A3A3 純裝飾。底色純白，交替區 `--greige-50` #F5F5F5（只剩 hover 在用），陰影中性灰。

**字標只有一個檔** `tsunagu-wordmark.png`（TSU #F3A0AA／NAGU #7DD0F5，從原檔逐像素換色，字形不動），
header 與 footer 共用。~~duo 深色版~~ 已刪（使用者認可主色版；logo 依 WCAG 1.4.3 豁免對比）。

### 動這套系統時的規則

1. ~~不要在主色底上放白字~~ 2026-09-13 使用者改判：**主按鈕就是粉底白字**（#F3A0AA 配白 2.0:1，其決定，不要再改回灰字）。其他主色底（圓章、頭像）仍用灰字。
   同理 `--ink-400`（2.52:1）只能當分隔點或邊框色，任何看得懂的文字都用 `--ink-600` 以上。
2. 中日文標題字距拉開（`letter-spacing: .05em`+）、字重不超過 700、行高 1.45 以上——這是日系感的來源，不是裝飾。
3. 陰影用中性淺灰（`rgba(70,70,70,.05–.10)`），不要用純黑或暖粉。
4. 動效 200–280ms，`cubic-bezier(.34,1.4,.64,1)`，且必須支援 `prefers-reduced-motion`。
   **例外：貼著視窗邊的東西（sticky header 的收合）不可用這條過衝曲線**——會滑過頭露出一條縫，用 `cubic-bezier(.22,.61,.36,1)`。
   **例外二：捲動進場動效**（2026-09-25 使用者要「絢麗」、每次捲到都重播）：`src/scripts/reveal.js` 依選擇器替元素標
   `data-rv`（rise／title／pop／card／slide／zoom／bounce），keyframes 在 global.css，.8–1s。以 `?raw` 讀進 Base.astro
   在 body 最後同步執行（首次繪製前就藏好，不閃）；完全離開畫面才重置。animation 用 `backwards` 不用 `both`，否則卡片 hover 會被鎖死。
   新增區塊要有動效：把選擇器加進 RULES（列表標子元素，不要標容器）。統計數字的跳動（motion.ts）同樣每次進場重播。
5. **形狀語言（2026-08 去 AI 味改版定案）**：`--r-full` 只給本來就圓的東西（頭像、
   輪播點、播放鈕）；按鈕 `--r-md`、標籤 `--r-xs`＋同色細框、公告列 `--r-sm`。
   **不要把任何文字容器改回膠囊形。**
6. 強調區塊只有兩種：`.note`（※ 起頭注釋，無底色）與 `.notice`（粉主色框必讀，
   全站每頁至多一個，只給免責／法遵／個資聲明）。**不要再發明第三種提示框，
   也不要用左側色條。**
7. **不要用 eyebrow**（標題上方的英文小字標）。標題自己承重。
   （2026-08-27 已把殘留的 `.eyebrow` 死碼刪除；apply 頁的序號改成圓形號碼章，
   同 events 的 `.step .no`——圓章是「本來就圓的東西」，不算違規。）
8. **字距/行高規則的適用範圍**（2026-08-27 與現況對齊）：`.05em+`/`1.45+` 指桌面
   d1/d2 級大標；d3 與手機版全級距刻意收窄（小字級拉大字距會散掉），這是定案不是違規。
9. **命名**：首頁統計列＝`.band`、stories/support 的漸層 CTA 盒＝`.cta-band`/`.free`，
   不要再讓兩者共用類名。企業卡（2026-09-13 參考 Cake／Yourator 重排）：品類圖標在品牌名左側同一列（不再有色帶），
    列序固定 名稱→登記名（名稱區固定 52px 高、內容垂直置中，無登記名時品牌名對齊圖標中線）→tagline（固定兩行高截斷）→標籤→實績（貼底），
    這樣同列各卡每一行都對齊；手機半寬卡藏登記名與 tagline。
    **對齊靠 CSS subgrid**（2026-09-24）：卡片 `grid-row:span 4; grid-template-rows:subgrid`（手機 span 3），
    列高由同列最高那張決定，品牌名折兩行也不會把旁邊的卡推歪；單欄要寫 `minmax(0,1fr)`，否則登記名（nowrap）會撐破卡。
    品牌名含假名（或 en 頁含漢字）時 `nameLang()` 會標 `lang="ja"`，日文走 `auto-phrase` 斷詞、不做 `keep-all`。
10. **首頁公告列（.news）只在「無下一場場次」時出現**（導向 IG）；有場次時 hero CTA
   已載明日期地點，公告列與其重複。影片區固定排在學長姐區之後（佐證素材，不搶核心資產位），
   並限寬 760px——不讓第三方新聞畫面成為全站最強視覺。
11. **頁首＝漢堡選單，桌機手機統一**（2026-09-13 使用者定案，推翻 8/28「不做漢堡」）：
   `logo ｜ 報名鈕 ｜ 語言 ｜ 選單鈕`，選單鈕在語言鈕右邊。選單鈕內是**粉藍兩條圓頭短線**，**無外框**（9/25 曾加 1px `--control-line` 框，
   2026-09-26 使用者再改回無框，語言鈕同；hover／開啟時淡灰底示意可點。線條對白 2.0／1.7:1 不達 1.4.11 為其決定）
   （logo 屋頂的左粉右藍），開啟時交叉成 X。抽屜：桌機是選單鈕下方的右對齊面板、手機全寬
   （鎖背景＋inert）。手機分頁列（.mobile-tabs）已移除，頁首 101px→56px。抽屜裡的「報名交流會」
   是**一般列＋粗體**，不做粉按鈕（使用者：突兀）。**當前頁＝粉字**（2026-09-14，不要左側色條）。
   桌機**滑鼠移到選單鈕即開**（離開鈕＋面板 160ms 後關；觸控維持點擊），面板**半透明＋backdrop blur**。
   手機抽屜開啟時下方加**半透明遮罩**（點遮罩關閉）。在 /events 時頁首報名鈕換成**次按鈕樣式**（標示當前頁）。
   /events 在抽屜、footer、`<title>`、h1 一律叫「報名交流會／交流会に申し込む／Join a meetup」（2026-09-25 統一）。
   **頁首收合**（同 kawausojapanese.com 的 Kadence 設定）：往下捲超過 96px 滑走（translateY -100%）、
   往上捲累積 12px 才回來（濾掉回彈抖動）、捲動後桌機 68→60px 並加淡陰影；選單或語言選單開著時不藏，
   鍵盤焦點進頁首時強制顯示。
   **毛玻璃畫在 `header::before`**，不要搬回 `header` 本身：帶 backdrop-filter 的元素會成為 backdrop root，
   抽屜面板的模糊就只取樣到 header、頁面文字直接透上來（2026-09-24 稽核）。`backdrop-filter` 只寫無前綴版，
   前綴交給 lightningcss（兩個都寫時 build 會丟掉無前綴那條）。
12. **統計列（.band）＝四個泡泡**（2026-09-13 使用者定案）：白底圓、2px 主色環粉藍交替（同頭像語言），
   泡泡內上排「圖標＋標籤」一列、下排粉數字（使用者指定的結構）。**全寬度一律一列四顆**；桌機泡泡上限 184px
   （2026-09-25 使用者：244px 時佔比太大），
   **手機（≤720）一列四顆**（2026-09-25 使用者：2×2 太佔空間）——圓裡第一行標籤、第二行數字，手機不顯示圖標
   （2026-09-26 使用者：原本標籤在圓外）；標籤字級 `clamp(9px,2.65vw,12px)` 隨寬縮放，390px 時中文標籤都能一行。
   標籤列 `flex-wrap:nowrap`（圖標不會被擠到上一行，長標籤在自己的 span 裡折行）、數字 `nowrap`（單位不分行）。
   兩個外部數字在 `site.ts` 的 `HOME_STATS`，三語共用。
   `--r-full` 用在「本來就圓」的泡泡上，不違反形狀語言。（曾試過框盒、無框磁磚、一主三次，皆被退回。）
   （舊註：iOS Safari 的 flex 直欄不把 min-height 算進置中，高度一律用 padding 撐。）
14. **跨頁共用的小元件在 `global.css`，頁面只加修飾類**（2026-09-19 審查後收斂：原本 `.cta-row`／號碼章／
   引言／CTA 盒／分隔線清單各頁各寫一份，gap 與 margin 悄悄分岔）：`.cta-row`、`.grid-2`、`.card-pad`、
   `.no-badge`（圓形號碼章）、`.quote`（「」引言，字級由頁面給）、`.cta-box`（白底大 CTA 盒）、
   `.rows`＋`.row`（分隔線清單，`.row .d` 是左側日期／編號欄）、`.note` 上距 22/18、`.closing`。
   **不要在 page-*.css 重新定義這些**；「安靜連結」也不再自訂，一律走全站 `a`（灰字＋藍底線）。
   `.action-link` 的「→」由 CSS `::after` 畫，markup 不要寫箭頭；外連的「↗」保留在 markup 但要
   `aria-hidden` ＋ sr-only「另開新視窗」（三語字串見各頁）。
15. **三語頁面的資料邏輯在 `src/lib/site.ts`**，不要在 ja/en 頁再抄一份：`signupCta()`（帶下一場日期的主 CTA）、
   `relatedStoriesFor()`、`eventLd()`、`fmtLocation()`（stories 地點日英表記）、`companyName()`（nameJa/nameEn）、
   `MEDIA`（媒體報導）、`FORM_URL`、`CONTRACTED_COMPANIES`、`totalHeld`（自動推導）；地區對照用 `ui[lang].area`。
   `events.json` 進場時有 zod 驗證，格式錯會在 build 擋下。
13. **品牌詳情頁不做「統一資訊表」**：地區/職種/日語/面試方式在標籤列、實績在數字磁磚、
   來源與時效在 .note，已涵蓋同樣欄位，再加表會同屏重複兩次。

---

## 內容資料在哪

| 內容 | 檔案 | 筆數 |
|---|---|---|
| 合作企業／品牌 | `src/content/companies/*.md` | 35 |
| 學長姐分享 | `src/content/stories/*.md` | 14 |
| 交流會場次 | `src/data/events.json` | 最近 12 場 |
| 角色圖 | `src/assets/tsunagu-*.png` | 3 |

企業與 stories 的 frontmatter schema 在 `src/content.config.ts`（Zod，寫錯 build 會擋下來）。

**數字口徑（2026-09-24 更新）**：對外統一用「自 2023 起累計協助 **168** 位
找到日本工作」、「累計 **3,253** 位參加交流會」、「**41** 家簽約企業」
（來源：第 88,89 回**交流會當天簡報**（2026.9.20 正式稿，非報名表單文案）第 188、173 頁）。
`/companies` 的人次加總只代表已揭露的 35 品牌，永遠加註「本頁品牌合計」。
~~163／2,874／39~~ 是 8/26 從報名表單文案定的舊口徑，9/20 當天簡報公布的實際數字
已更新（見同日 commit）；簡報上的「149 人正在日本工作」（在職口徑）仍棄用不用。

**資料來源：**
- 企業與統計數字 → 《第88,89回 台北交流会スライド2026.9.20.pdf》（229 頁，
  放在 `~/Downloads/`，是當天使用的**正式稿**，非「途中」草稿）。
  ~~《第76,77回…2026.4.18【途中】.pdf》~~（196 頁，`~/Desktop/Tsunagu/`）已由此取代，
  但仍留著給尚未走到新稿的品牌頁對照舊 sourceSlide 頁碼。
  ⚠️ **三語有出入時，一律以最新一份簡報載明的為準**（2026-08-28 使用者定案）。
  新稿沒有文字層可全文搜尋（多數投影片是圖片），已用 pypdf 抽出可抽取的文字段落逐頁比對；
  沒有文字的頁（多為純照片）無法自動核對，換稿時這類頁面的數字需要人工開圖覆核。
  已據 9/20 新稿對齊：AOKI/ORIHICA 從「日本TOP2」改為簡報明載的「日本No.1」（p145，
  用語是簡報自己變了，非本站誤植）、9 個品牌的累計人數（beams／manhattan-portage／
  vasic／mukai／graniph／aoki-orihica／override／tabio／lindt）、每月訪日人次
  55–62 萬→66–76 萬（p12）。**尚未逐一開圖核對的品牌**（沒有可抽取文字、無法確認是否
  有異動）：cupid-heart、blooming-nakanishi、sazaby-league 子項、以及簡報 p173
  「現在41社」名單裡對不上任何現有品牌檔的公司（至少 WEGO／株式会社ウィゴー，p79-80
  有完整資料，尚未建檔——新增品牌需要的欄位都在，但這輪先只修正既有品牌數字，
  新品牌留待下次處理）。
  歷史對齊項目（4/18 稿）：勞動條件的承諾強度（簡報 p159 是「向企業提出」而非法定保證）、
  レオパレス（p180，無 21）、協會 1,000 社為確數（p18）、きものやまと 表記（p13/p24/p100）。
  出處字串請用 `fmtSourceDeck()`（`src/lib/site.ts` 的 `SOURCE_DECK`），不要在頁面各寫一套。
  **各品牌 `sourceSlide` 已於 2026-09-24 逐頁核對成 9/20 版（229 頁）的頁碼**（原本 26 個還是 4/18 版頁碼）：
  有人數的那一頁優先；圖片頁無文字層者取品牌段落首頁（blooming-nakanishi 126、festaria 107）。
  換新簡報時頁碼要整批重核，不能只改 `SOURCE_DECK`。
- 學長姐 → IG 上「\ 透過ツナグ介紹🇯🇵在日打工中的台灣朋友/」系列貼文，摘錄＋回連原文，未整篇轉載。
- 場次 → IG 貼文的「第 N 回…開催しました」解析而來。

**新增一場交流會**：在 `src/data/events.json` 的 `events` 陣列最前面加一筆。
日期過了會自動從「下一場」移到「舉辦紀錄」，不用手動搬。
⚠️ 每一場的 Google 報名表單網址都不同，`signupUrl` 要逐場填。
`totalHeld`（累計場數）只是下限：`site.ts` 會拿「已過期場次的最大回數」取大者，場次過了首頁泡泡自動跟上（2026-09-19）。
簽約企業數 39 在 `site.ts` 的 `CONTRACTED_COMPANIES`，三語 /companies 共用。

---

## 絕對不要做的事

1. **不要代寫學長姐的引言。** 所有引述都必須來自 IG 原文，並附 `igCode` 回連。編造真人發言不可接受。
2. **不要在官網放即時職缺狀態。** 顯示「募集中」但實際已滿會直接燒掉信任。只放品牌介紹與過往實績，職缺導向交流會。
3. **不要貼媒體報導截圖。** 那是報社著作。用「標題＋日期＋外連原文」。
4. **不要讓官網蒐集個資。** 表單一律外連或嵌入 Google Forms。
5. **不要使用任何企業 logo**，除非確認拿到授權。目前卡片用漸層底＋品牌名文字。

---

## 上線前必須解決的

### 1. Footer 的許可資訊（最高優先）

台灣《就業服務法》第 34 條：私立就業服務機構須經許可才能從事就業服務業務。
《私立就業服務機構許可及管理辦法》第 35 條：**就業服務業務廣告應載明
「機構名稱、許可證字號、機構地址及電話」**。

**這四項要填持照方的資料。** 實際媒合業務由持有就業服務機構許可的合作方執行，
footer 揭露的是該持照方，不是網站主辦單位。正確資料請向該合作方索取。

上線前要一併確認：
- 許可證**有效期限**（第 25 條，效期 2 年）——過期即屬不實廣告（第 40 條）
- 該許可的經營業務項目**是否包含「仲介本國人至中華民國境外工作」**
- 個資的蒐集者／保有者是哪一方（影響隱私權條款要用誰的名義）

程式碼位置：`src/components/Footer.astro`。
⚠️ 2026-08 使用者決定畫面上先不放佔位框（未填的【待補】黃框對訪客無意義），
所以**畫面上已看不到任何提醒**——佔位段落在 git 歷史裡（搜 `.pending`）。
這條待辦現在只靠本文件與 Footer.astro 內的 HTML 註解存活，上線檢查清單必須包含此項。

> 這不是法律意見，是提醒去確認的方向。實際條文與適用請洽律師或勞動部。

### 1-b. 品牌內容的性別敘述（2026-08-28 已處理，但需主辦方複核）

barbour／paraboot／yamato 三個品牌檔原本載有**指名性別的招募條件**
（「積極徵求女性」「歡迎男性（女性亦可）」「積極徵求男性」），已移除。
台灣《性別平等工作法》第 7 條、《就業服務法》第 5 條與日本《男女雇用機会均等法》
第 5 條都禁止招募階段的性別差別待遇，而這些字串三語都會原文渲染。

**只做刪除、未新增任何敘述。** 請向主辦方確認：資料源（2026.4.18 簡報）若仍載有
性別條件，往後轉錄時一律不要帶入。`beams.md` 的「東京 5 位女性…」是過往實績
而非招募條件，暫予保留，但如要對外揭露性別組成請一併確認。

### 2. 其他【待補】

⚠️ 2026-08 起**畫面上已無任何黃框**（含 stories/apply/about 的【待補】，
與 Footer 相同方針）。以下待辦只存在於本文件與各檔案內的 JSX 註解
（搜「內部待辦」），上線檢查清單必須逐項核對：

- ~~`FORM_URL`~~ 2026-08-26 確認：**線上收件流程從未建立過**。apply 頁已重定位為「交流會現場／IG 私訊」雙管道導流；若日後建立 Google 表單，填入 `FORM_URL` 即自動切換
- 隱私權政策與**跨境傳輸同意條款**（資料會傳到日本）
- `src/data/events.json` 的下一場場次
- ~~媒體報導原文連結~~ 2026-08-26 已查：台北經濟新聞與繊研 2026.01.28 已外連原文；
  日經 2024.10.07 與繊研 2025 兩篇為紙面刊載（senken.co.jp 無線上版，實測 404），已標示。
  ~~「Yahoo! ニュース 2024.08.07」存疑~~ 2026-08-28 已解除：**簡報 p22 的媒體名單
  明載「日本の繊研新聞／台北経済新聞／Yahoo!ニュース」**，Yahoo 一項屬實。
  同時修正一處錯置：簡報 p24 把「きものやまと 的 Haru」繫於 **2024.08.07**，
  網站原本掛在日經 2024.10.07（p25）上，三語已移正
- 學長姐內容轉載到官網是否需另外取得本人同意

### 3. 尚未做的

- ~~日文版~~ 2026-08-27 已做：**三語整站**（繁中 `/`、日文 `/ja/`、英文 `/en/`，127 頁），
  header 右上有語言選單，hreflang 三語互指＋x-default。**維護原則：**
  - 繁中頁是母版；ja/en 頁為獨立檔案（`src/pages/ja|en/…`），**改版面結構時三語要同步改**
  - 頁面樣式抽在 `src/styles/home.css` 與 `page-*.css`（三語共用，改一份三語生效）
  - 元件層字串（導覽/footer/卡片標籤）在 `src/i18n/ui.ts`；品類/城市三語在 `src/lib/site.ts`
  - **互動邏輯抽在 `src/scripts/`**（如 `company-filter.ts`），三語頁面 import 同一份，
    顯示字串由頁面的 `data-*` 傳入。**不要再把 JS 複製到各語言頁**（原本三份複製，改一次漏兩處）
  - 品牌 tagline 的日英在各 md 的 `taglineJa/taglineEn`
  - **學長姐引言、messages、advice 一律保留繁中原文不翻譯**（紅線 #1：翻譯也是改寫），
    ja/en 頁已標註「原文刊載」；品牌頁 prose 內文與 highlights 暫同（標註原文），翻譯屬後續工作
  - **用語：全站日文用「人材」，不用「人財」**（2026-08-27 使用者定案。B&P 官方對外資料用
    「人財」，但使用者選擇較中性的「人材」——不要改回去）
  - 2026-08-27 三位翻譯審查員的語言層修正已全數套用（文法、敬語、表記統一、
    免費範圍限定「台湾の皆さまからは」、法遵表述「法令に基づき同一の労働条件」等）；
    同日第二輪全站複查（日文複查＋繁中英文＋技術）修正亦已套用
  - **表記方針**：頁面本文的數字前後留半形空格（「163 名」「8 年」）；
    品牌卡 tagline 刻意採「詰め」（「1998年」「創業77年」）——兩者都是既定方針，不要互改
  - **場次編號**「88,89」資料源保持逗號；顯示層用 `fmtEventNo()`（zh「、」ja「・」en「–」）
- **第二輪稽查的低優先遺留**（上線後再處理即可）：
  - 站內連結無結尾斜線（GH Pages 會 301 到有斜線版）：`href()` 補斜線或設 `trailingSlash`
  - 404 頁的語言選單指向不存在的 /ja/404/、/en/404/（實際會回退同一張 404，無害）
  - en 頁殘留日式點分日期（since 2023.09、2026.4.18、2019–2025.9）
  - en stories 的 location 仍為繁中原值（已標 lang="zh-Hant"；譯名屬後續工作）
  - ~~首頁 stats 三語各寫死一份~~ 2026-09-24 已抽到 `site.ts` 的 `HOME_STATS`（簽約企業數在 `CONTRACTED_COMPANIES`），各改一處即可
  - events「直近 N 回」以 JSON 筆數計，一筆＝一天（可含兩回），與官方回數口徑略有落差
- **日文版戰略缺口**（2026-08-27 審查員一致指出；**全部需要主辦方提供資料，不可代造**）：
  - 「企業のご担当者様へ」頁：ja 版另一半讀者是日本企業，但全站導覽零企業入口。
    需要主辦方提供：提攜流程、費用結構、（如適用）有料職業紹介許可番號
  - 会社概要表（所在地・代表者・設立・資本金・電話）：日本企業做背景調查的必要件
  - 法人聯絡窗口：目前全站只有 IG DM，對企業讀者等於沒有窗口
  - 「日本ショッピングセンター協会の運営に参画」的具體關係（會員？委員？）待主辦方確認
  - 日經 2024.10.07 報導中「Haru さん」的表記方式，與該員工本名揭露範圍，向主辦方確認
- **首頁影片＝直接嵌入 iframe（2026-08-28 使用者定案，推翻同日稍早的門面方案）**：
  使用者要「一次點擊就能播」並指定起始秒數。起始秒數在 `src/lib/site.ts` 的
  `VIDEO_START`（三語共用）。代價（已知且接受）：載入首頁即拉約 4.2MB 並向
  Google 送出訪客 IP。維持 youtube-nocookie.com；仍不要用 i.ytimg.com 縮圖
  或新聞畫面截圖當封面（紅線 #3）
- **`--control-line`（#8A8A8A）是控制項邊界專用**：搜尋框、未選中的篩選 chip
  都要用它才有 WCAG 1.4.11 的 3:1。`--line` 只給髮絲分隔線。
  篩選 chip 的規則必須寫成 `button.tag.f`，權重才壓得過 `.tag.area/.job/.jlpt`
- **`.headline-part{white-space:nowrap}` 只適用中日文**：英文標題較長，
  nowrap 會撐破手機的版面視窗（`scrollWidth` 量不到這種溢出，要看 `innerWidth`）
- **FAQ 區塊與 LINE 諮詢入口**（2026-08-28 外部建議，已同意但缺料）：
  - FAQ 需要主辦方提供真實 Q&A（費用怎麼算、日語不夠怎麼辦、簽證要多久…）。
    **不可自行編寫答案**——那是杜撰服務內容，等同紅線 #1 的性質
  - LINE：目前只確認 IG 與 Threads 兩個官方帳號。有官方 LINE 才加，不要放個人帳號
- GA4（裝了就需要 cookie 同意）
- 角色原始檔目前只有 761px PNG，首頁最大顯示 380px。拿得到 SVG 就直接覆蓋 `src/assets/` 三個檔
- **OG 縮圖**（2026-09-13 重製）：`scripts/og/og.html` 用 HTML 排版（愛心＋字標＋標語＋角色＋底部粉藍雙色線），
  `node scripts/og/render.mjs` 以 Playwright 截成 2400×1260 的 `public/img/og-default.png`。改標語或字標後重跑。
  LINE／FB 會快取舊縮圖，換圖後要用各平台的 debugger 重新抓取（或換一次網址 query）

---

## 指令

```bash
npm install
npm run dev      # http://localhost:4321
npm run build    # → dist/
npm run preview
```

### Artifact 預覽鏡像（2026-09-13）

使用者要一個在 Claude 裡直接看的同步版：<https://claude.ai/code/artifact/497c86fd-9c5a-4a57-bdc4-5f6039175f01>
- `scripts/mk-artifact.py .artifact` 把 `dist/` 轉成相對路徑版（Artifact 不吃根路徑；
  `_astro/` 是保留字所以改名 `assets/`），輸出到 `.artifact/`（已 gitignore）
- 然後用 Artifact 工具以 `file_path: .artifact/index.html`、`root: .artifact`、`files` 為
  其餘 156 檔的 map 重新發布——**同一個 file_path 才會更新同一個 URL**
- **每次 push 部署後都要同步一次**，否則鏡像會落後線上版
- 已知限制：YouTube iframe 在 Artifact 裡被 CSP 擋掉（顯示空白），其餘功能正常

### 部署現況（2026-08-28 已上線）

- 倉庫：<https://github.com/kawausojp/tsunagu-site>（public）
- 網址：<https://kawausojp.github.io/tsunagu-site/>
- 推 `main` 自動觸發 `.github/workflows/deploy.yml`（另有每日 00:17 台北時間排程重建，
  讓過期場次自動移到「舉辦紀錄」）。Pages 來源已設為 GitHub Actions，不用再手動設定。

⚠️ **目前全站 noindex（不被 Google 收錄）**：`deploy.yml` 建置步驟設了 `TSUNAGU_NOINDEX: '1'`，
Base.astro 讀到就對 127 頁輸出 `robots: noindex,follow`。
**這是刻意的**——footer 的就業服務許可資訊（機構名稱／許可證字號／地址／電話）尚未填入，
見下方「上線前必須解決的」。**資料補齊並確認可正式對外後，刪掉 deploy.yml 那兩行即可公開收錄。**
（robots.txt 維持 `Allow: /`：要讓爬蟲讀得到 noindex，設 Disallow 反而讀不到。）

`astro.config.mjs` 開頭的 `site` / `base` 要依實際網域改：

| 情況 | site | base |
|---|---|---|
| 自訂網域 | `https://tsunagu.tw` | `/` |
| `<帳號>.github.io/<repo>` | `https://<帳號>.github.io` | `/<repo>` |

**`base` 沒設對，CSS 和圖片會全部 404。**

---

## 已知的小事

- BIZ UDPGothic 只涵蓋日文漢字，繁中專用字（灣、臺、燈…）會 fallback 到 Noto Sans TC。視覺上接近，但不完全一致。
  所以**繁中引言 `.quote:lang(zh-Hant)` 改走本文字（Noto Sans TC）**（2026-09-24）：原本一句引言兩種字形混排，
  stories 頁還因此多抓約 2.4MB 字型。
- **ja 頁本文用 BIZ UDPGothic**（2026-09-19）：`--f-body` 以 Noto Sans TC 優先是為 zh 設的，ja 頁若沿用會出現台式字形；
  ja 頁內 `lang="zh-Hant"` 的引言區塊仍回到 TC。en 頁整頁 Jost，所以 Google Fonts 要載 Jost 400/500（只載 600/700 時內文會變半粗體）。
- 導覽的 /apply 標籤是「準備出發／出発準備／Getting Ready」（2026-09-19）：原「資料提交」承諾線上送件，但收件流程不存在（見上方 FORM_URL）。
- **日期表記**（2026-09-25 使用者定案）：完整日期一律 `2026.01.28`（點分、月日補零），用 `site.ts` 的
  `fmtDate()`／`fmtEventDateFull()`；CTA 裡的月日短式「10/18（日）」不在此列。
- **字標只有一個檔** `tsunagu-wordmark.png`（2026-09-13 重製：TSU #F3A0AA／NAGU #7DD0F5，主色版），
  header 與 footer 共用。~~duo 深色版~~ 已刪。header 曾用 Jost 文字模擬字標，已改掉——
  **不要再改回文字版**。重製方法：純 Python 讀寫 PNG、按覆蓋率換色，見同日 commit。
- 角色圖是從白底 PNG 程式去背的（線稿有斷口，用了膨脹→填滿→侵蝕）。若換新圖需重做去背。
- IG 上出現但不在簡報名單裡的品牌：**NERGY**、**WUMEI 日式台灣食堂**。尚未加入，待確認是否仍在合作。
- **WEGO**（株式会社ウィゴー）：9/20 新稿 p79-80 有完整資料（1994 年創業、日本約 180 店、
  今年台灣進軍誠品生活武昌/板橋店、東京大阪各 1 人任職），是簡報 p173「現在41社」名單裡的
  正式簽約企業，非 IG 推測，之後要加品牌檔時直接用。
